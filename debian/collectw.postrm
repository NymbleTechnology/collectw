#!/bin/sh -e

# Automatically added by dh_installinit
if [ "$1" = "purge" ] ; then
	# purging debconf data
        db_purge || true
        update-rc.d collectw remove >/dev/null || exit $?
fi
# End automatically added section
