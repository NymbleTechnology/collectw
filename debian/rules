#!/usr/bin/make -f

clean::
	dh_clean
	rm -f stamp/*
	rm -f debian/collectw.substvars
	touch $@

.PHONY: build install clean
.PRECIOUS:

build:

install:

proper:
	dh_clean

binary-arch: proper build install
	dh_testdir
	dh_testroot
#	dh_installinit --name=collectw --onlyscripts --restart-after-upgrade
#	dh_strip --dbg-package=collectw-dbg
	dh_installchangelogs
	dh_installdocs
	dh_installexamples
	dh_installman
	dh_installdebconf -n
	dh_compress
	dh_fixperms
	dh_installdeb
	dh_shlibdeps
	dh_gencontrol
	dh_md5sums
	dh_builddeb

binary: binary-arch
